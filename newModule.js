var extFun = (arr) => {
    return `This array contain ${arr.length} elements`;
}

var adder = (a,b) => {
 return `result is ${a+b} nice`;
}

var pi = 14.73;

module.exports = {
    extFun : extFun,
    adder : adder,
    pi : pi
}